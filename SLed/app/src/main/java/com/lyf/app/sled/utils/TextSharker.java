package com.lyf.app.sled.utils;

/**
 * Created by yunfeng.l on 2017/11/10.
 */

public class TextSharker {
/*
    public int color_cnt=0;
    private void shark() {
        Timer timer = new Timer();
        TimerTask taskc = new TimerTask() {
            @Override
            public void run() {
                runOnUiThread(new Runnable() {
                    @Override
                    public void run() {
                        if(color_cnt == 0)
                        {
                            color_cnt =1;
                            marqueeView.setTextColor(getResources().getColor(R.color.colorSurfaceBackgroud));
                        } else {
                            color_cnt =0;
                            marqueeView.setTextColor(getResources().getColor(R.color.colorMarqueeText));
                        }
                    }
                });
            }
        };
        timer.schedule(taskc, 1, 500);
    }
    */
}
